---
description: Conventions for building public API Route Handlers accessible to MCP and 3rd-party systems
globs: src/app/api/**/*.ts
alwaysApply: false
---

# API Route Handlers (App Router)

This project uses **App Router Route Handlers** — not Pages Router API routes. Route files live at `src/app/api/<resource>/route.ts`.

> These routes are the primary integration point for MCP servers and other 3rd-party systems. The Pages Router (`pages/api/`) is not used.

## File Structure

```
src/app/api/
  decks/
    route.ts          # GET /api/decks
    [id]/
      route.ts        # GET /api/decks/:id
  cards/
    route.ts          # GET /api/cards
    [id]/
      route.ts        # GET /api/cards/:id
```

## Route Handler Anatomy

```typescript
// src/app/api/decks/route.ts
import { auth } from "@clerk/nextjs/server";
import { NextRequest, NextResponse } from "next/server";
import { getDecksByUser } from "@/db/queries/decks";

export async function GET(req: NextRequest) {
  const { userId } = await auth();
  if (!userId) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  try {
    const decks = await getDecksByUser(userId);
    return NextResponse.json({ decks });
  } catch {
    return NextResponse.json({ error: "Internal server error" }, { status: 500 });
  }
}
```

## Auth in Route Handlers

Route handlers must **return a 401 JSON response** for unauthenticated requests — never `redirect()`.

```typescript
// ✅ GOOD — return 401 for route handlers
const { userId } = await auth();
if (!userId) {
  return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
}

// ❌ BAD — redirecting inside a route handler
if (!userId) redirect("/");
```

## CORS for MCP / 3rd-Party Access

Add CORS headers to any route that must be callable by external systems (e.g. MCP servers).

```typescript
const CORS_HEADERS = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Methods": "GET, POST, OPTIONS",
  "Access-Control-Allow-Headers": "Content-Type, Authorization",
};

// Handle preflight
export async function OPTIONS() {
  return new Response(null, { status: 204, headers: CORS_HEADERS });
}

export async function GET(req: NextRequest) {
  const { userId } = await auth();
  if (!userId) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 }, { headers: CORS_HEADERS });
  }
  // ...
  return NextResponse.json({ data }, { headers: CORS_HEADERS });
}
```

## Data Access

Route handlers must delegate to query helpers in `src/db/queries/` — never write inline Drizzle queries.

```typescript
// ✅ GOOD
import { getDecksByUser } from "@/db/queries/decks";
const decks = await getDecksByUser(userId);

// ❌ BAD — inline Drizzle query
const decks = await db.select().from(decks).where(eq(decks.clerkUserId, userId));
```

## Dynamic Route Segments

```typescript
// src/app/api/decks/[id]/route.ts
export async function GET(
  req: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) {
  const { userId } = await auth();
  if (!userId) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  const { id } = await params;
  const deck = await getDeckByIdAndUser(Number(id), userId);

  if (!deck) {
    return NextResponse.json({ error: "Not found" }, { status: 404 });
  }

  return NextResponse.json({ deck });
}
```

## Rules

- Route handlers live in `src/app/api/` — never create `pages/api/` files
- Always call `auth()` and return `401` when `userId` is null — never redirect
- Always delegate data access to `src/db/queries/*` — never inline Drizzle queries
- Always scope queries to `userId` — never expose another user's data
- Add `OPTIONS` handler and CORS headers on any route consumed by external systems
- Use `NextRequest` / `NextResponse` from `next/server`
- In Next.js 15+, `params` is a `Promise` — always `await params` before destructuring

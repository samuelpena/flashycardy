import Foundation

/// Stable identifiers for XCUITest (not localized).
enum AccessibilityID {
    enum Auth {
        static let appName = "auth.appName"
        static let signIn = "auth.signIn"
        static let signUp = "auth.signUp"
    }

    enum Dashboard {
        static let title = "dashboard.title"
    }
}
